# 把本文件复制为 deployment/hosts.env；hosts.env 已被 Git 忽略，不会上传服务器地址或私钥路径。
# 所有 *_HOST 使用“从 Mac SSH 连接到服务器”的地址。通常填写公网 IP；若 Mac 已接入同一内网，也可填写内网 IP。
# 示例：MASTER_HOST='47.96.10.21'
MASTER_HOST=''

# Master 在同一 VPC/子网中的内网 IPv4。NFS、Kubernetes 节点通信使用它，不能填写公网 IP。
# 示例：MASTER_PRIVATE_IP='172.16.0.10'
MASTER_PRIVATE_IP=''

# Node 01 的 SSH 地址，填写公网 IP 或 Mac 可直连的内网 IP。
# 示例：NODE01_HOST='47.96.10.22'
NODE01_HOST=''

# Node 01 的 VPC 内网 IPv4。
# 示例：NODE01_PRIVATE_IP='172.16.0.11'
NODE01_PRIVATE_IP=''

# Node 02 的 SSH 地址，填写公网 IP 或 Mac 可直连的内网 IP。
# 示例：NODE02_HOST='47.96.10.23'
NODE02_HOST=''

# Node 02 的 VPC 内网 IPv4。
# 示例：NODE02_PRIVATE_IP='172.16.0.12'
NODE02_PRIVATE_IP=''

# Mac 本机 SSH 私钥的绝对路径。这是“文件路径”，不是把私钥内容粘贴到这里。
# 示例：SSH_KEY='/Users/weibin/.ssh/aliyun-k8s.pem'
SSH_KEY=''
